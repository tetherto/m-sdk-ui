{
  "name": "mining-sdk-ui",
  "version": "0.0.0",
  "private": true,
  "packageManager": "pnpm@10.5.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=10.0.0"
  },
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "build:verbose": "turbo build --output-logs=full",
    "lint": "turbo lint",
    "lint:fix": "turbo lint:fix",
    "format": "turbo format",
    "typecheck": "turbo typecheck",
    "check": "turbo check",
    "fullcheck": "pnpm lint && pnpm typecheck && pnpm format && pnpm build",
    "clean": "turbo clean && rimraf node_modules",
    "prepare": "husky"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^6.7.3",
    "@vitejs/plugin-react": "^5.1.4",
    "concurrently": "^9.2.1",
    "eslint": "^9.27.0",
    "eslint-plugin-format": "^1.3.1",
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "rimraf": "^6.0.1",
    "sass": "^1.97.3",
    "turbo": "^2.5.4",
    "typescript": "^5.8.3",
    "vite": "^7.3.1"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx,mjs,cjs}": [
      "eslint --fix"
    ]
  }
}
